const double galleryHeaderHeight = 64;

const double desktopSettingWidth = 520;

const double systemTextScaleFactorOption = -1;

const Duration splashPageAnimationDuration = Duration(milliseconds: 300);

const Duration settingsPanelMobileAnimationDuration = Duration(milliseconds: 200);

const Duration settingsPanelDesktopAnimationDuration = Duration(milliseconds: 600);
